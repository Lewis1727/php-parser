<h3 class="text-center">FILMS</h3>

<p>
  <button class="btn btn-dark" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
    Select file to upload
  </button>
</p>
<div class="collapse" id="collapseExample">
        <form action="upload.php" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <input type="file" class="form-control-file" name="fileToUpload" id="fileToUpload">
                <button type="submit" class="btn btn-primary mb-2"style="margin-top: 15px;">Upload File</button>
            </div>
        </form>
</div>


<div class="row">

    <table class="table">
        <thead class="thead-dark">
            <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Year</th>
            <th scope="col">Format</th>
            <th scope="col">Actors</th>
            <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            <?php
            $escapeHtml = 'htmlspecialchars';
            $posts = isset($variables) ? $variables : [];
            $q=0;
            foreach ($posts as $postArray)
            {
            $q++;
            ?>
            <tr>
                <td><?php echo $q; ?></td>
                <td><?php echo $escapeHtml($postArray['name']); ?></td>
                <td><?php echo $escapeHtml($postArray['year']); ?></td>
                <td><?php echo $escapeHtml($postArray['format']); ?></td>
                <td><?php echo $escapeHtml($postArray['actors']); ?></td>
                <td><a class="text-danger" href="/?action=delete&id=<?=($postArray['id']);?>">DELETE</a></td>
            </tr>
            <?php } ?>
        </tbody>
    </table>
</div>

<?php

// $xml = simplexml_load_file("document.xml");
// print_r($xml);

?>